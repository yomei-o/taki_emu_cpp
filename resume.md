# resume.md — 次に触るときの覚書

最終更新: 2026-08-17

## いまどうなっているか

滝のシミュレータ。**水を粒子で解いて、軌跡をそのまま線として焼き付ける**（teamLab の滝と同じ考え方）。
動いている。ユーザ評価は「めっちゃいい感じ」。

- GitHub: https://github.com/yomei-o/taki_emu_cpp （main に push 済み）
- デモ: https://yomei-o.github.io/taki_emu_cpp/wasmdist/taki_os/

## すぐ動かす

```sh
cd /c/prog/claude/taki_emu_cpp
EMSDK=/c/prog/emsdk/emsdk ./build.sh taki_os

clang++ -O2 -std=c++17 -I. -o taki_test.exe src/taki_os.cpp
./taki_test.exe 600 2 out.png                  # 引数: フレーム数 岩の形 出力PNG
CAM="0 80 55 900" ./taki_test.exe 500 2 top.png   # 真上から（診断用）"az el R f"
DECAY=0.5 ./taki_test.exe 500 2 now.png           # 尾を消して「今の水」だけ見る
```

---

# 課題

## 1. 【未解決】着水速度が √(v₀²+2gh) より約25%高く出る

画面下に「実測 落差 → 着水速度（√(v₀²+2gh) = …）」と並べて出しているが、
**実測が理論値より 24.4 対 19.1 と高い**。原因が特定できていない。

**すでに潰した仮説**（どれも違った）:

- 圧力を**完全に切っても**変わらない（`KP=0` で検証済み）→ 圧力ではない
- 法線の**頭打ちを外しても**変わらない（`GMAX` を 5→12→40 で不変）→ めり込み補正のポンプでもない
- 平均をサブステップごとの平均 → **粒子で重み付け**に直しても変わらない → 統計の取り方でもない
- 反射の**前**に速さを測るよう直した（後だと滑る速さになる。8.2 m/s に見えていた）→ これは直った

**次にやること**: 個別の粒子について (v, v₀, h) を1件ずつ吐いて、どこで帳尻が合わなくなるか見る。
1粒子を追跡して、接地→離陸→着地の1サイクルでエネルギーが合うかを直接確かめるのが早い。
`d.ytop` / `d.v0` は接地のたびに引き直しているので、そこの取りこぼしを疑う。

## 2. 見た目

- 滝壺で水が横に広がるのが perspective で強調されて「スカート」に見える
- 岩のワイヤ（等高線）はもう少し情報量を足したい

## 3. 速度

未計測。プロファイルを入れていない（umi_emu_cpp には入っているので移植すると早い）。

---

# 踏んだ罠

- **発生点に流量を詰め込むと圧力で吹き飛ぶ。** 全粒子を落ち口手前の狭い箱に置いたら
  1セルに2000粒たまり、水が横に噴いて「蝶の羽」になった。上流の川**全体**に配ること。
  流量 Q = 幅×深さ×流速 が成り立つ深さで入れる必要がある
- **密度の基準 REST は粒子数から決める。** 固定値にすると粒子を増やしただけで爆発する。
  `REST = N/700`、`KP = 110/REST`（K·REST ＝ 水の固さ を一定に保つ）
- **滞留時間の配分がそのまま絵の密度になる。** 滝壺に居座らせると滝が痩せる。
  滝壺の粒子は 1.15 秒で回収している
- **筋は描かない。** 高さ場に溝（ガリー）を彫ると水が自分で集まって筋になる。溝3本で三筋の滝
- swap-remove の採番し直しで死ぬ（外に控えたインデックスも一緒に付け替える）
- Windows の bash ヒアドキュメントで C 文字列の `\n` が本物の改行に潰れる。Edit ツールを使うこと
